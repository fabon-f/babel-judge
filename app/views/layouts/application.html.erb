<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <%= display_meta_tags site: 'Babel Judge', reverse: true %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "application", "data-turbo-track": "reload" %>
    <%= vite_stylesheet_tag "application" %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag "application"

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag "application.jsx"

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->
  </head>

  <body>
    <header>
      <nav class="flex justify-between px-6 py-4">
        <a href="<%= root_path %>" class="font-bold">Babel Judge</a>
        <ul class="flex gap-4">
          <% if current_user %>
            <li><a href="<%= user_path(current_user) %>"><%= current_user.username %></a></li>
            <li><a href="<%= sign_out_path %>">サインアウト</a></li>
          <% else %>
            <li><a href="<%= sign_up_path %>">ユーザー登録</a></li>
            <li><a href="<%= sign_in_path %>">サインイン</a></li>
          <% end %>
        </ul>
      </nav>
    </header>
    <% if flash[:notice] %>
      <div class="max-w-4xl mx-auto mt-4 border border-blue-600 bg-blue-100 text-blue-600 p-4 rounded">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="max-w-4xl mx-auto mt-4 border border-red-600 bg-red-100 text-red-600 p-4 rounded">
        <%= flash[:alert] %>
      </div>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
